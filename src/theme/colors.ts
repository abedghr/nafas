// Single import point for theme. New code imports from "@/src/theme".
// Re-exports the existing palette (constants/colors.ts) so we don't duplicate.
export { default as Colors } from "@/constants/colors";
